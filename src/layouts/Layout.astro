---
import { Head, Nav } from "./../components/index";
import "./../styles/styles.sass";

interface Props {
    title: string;
}

const { title } = Astro.props;
---

<html lang="en">
    <Head title={title} />
    <body>
        <slot />
        <Nav />
    </body>
</html>
